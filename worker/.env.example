# A KyloKit worker konfigurációja a saját VPS-en.
# Másold ezt .env-re és töltsd ki.

# A Lovable Cloud-on futó Brain publikus címe (custom domain vagy lovable.app):
BRAIN_URL=https://brain.kylosystems.com

# Megosztott titok a worker <-> Brain hitelesítéshez.
# Pontosan ugyanaz az érték, mint a Lovable secret store-ban (WORKER_API_TOKEN).
WORKER_API_TOKEN=REPLACE_WITH_TOKEN

# Saját azonosító (csak logolásra), pl. worker-1, worker-budapest stb.
WORKER_ID=worker-1

# Hány workflow fusson párhuzamosan ezen a gépen.
MAX_PARALLEL=4

# Milyen sűrűn nézzen új jobot (ms).
POLL_INTERVAL_MS=3000

# ---- Recorder (élő felvétel) finomhangolás ----
# A recorder NEM igényel külön Supabase kulcsot — minden credentialt
# a Brain-től kap a record-claim válaszban. Csak BRAIN_URL és
# WORKER_API_TOKEN kell neki, ugyanaz mint az orchestratornak.
RECORD_POLL_INTERVAL_MS=2000
RECORD_MAX_SESSIONS=2
RECORD_FPS=5
RECORD_VIEWPORT_W=1280
RECORD_VIEWPORT_H=800
